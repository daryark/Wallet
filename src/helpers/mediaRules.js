// import { useMediaQuery } from 'react-responsive';

// function WithMediaRules(Component, size) {
//   const ComponentWithMediaRules = props => {
//     const isMobile = useMediaQuery({ maxWidth: 767 });
//     const isTablet = useMediaQuery({ maxWidth: 1279 });
//     const isDesktop = useMediaQuery({ minWidth: 1280 });

//     if (size === 'sm') return isMobile && <Component {...props} />;
//     if (size === 'md') return isTablet && <Component {...props} />;
//     if (size === 'lg') return isDesktop && <Component {...props} />;
//   };

//   return ComponentWithMediaRules;
// }

// export default WithMediaRules;
